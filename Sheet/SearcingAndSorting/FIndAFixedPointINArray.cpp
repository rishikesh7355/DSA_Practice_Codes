#include <bits/stdc++.h>
using namespace std;

int main() {
    int arr[5] = {15,2,45,12,7};
    int n=sizeof(arr)/sizeof(arr[0]);
    int ans[n];
    for(int i=0;i<n;i++){
        if(arr[i]==i+1){
            cin>>arr[i];
        }
    }
    if(arr[])
}